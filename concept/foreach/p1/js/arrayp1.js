"use strict";

var colors = ["red", "blue", "green", "yellow", "brown"];

for (let color of colors) {
  console.log(color);
}

// Using forEach function instead of traditional for loop
colors.forEach(function (color) {
  console.log(color);
});

